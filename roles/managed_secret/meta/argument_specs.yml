---
argument_specs:
  main:
    short_description: Manage Secret
    options:
      managed_secret_name:
          type: "str"
          required: true
          description: managed secret name
      managed_secret_value:
          type: "str"
          required: false
          description: secret value (default random)
